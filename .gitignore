node_modules
dist
.turbo
.next
.cache
coverage
*.tsbuildinfo

# editor / OS
.DS_Store
.vscode
.idea

# env
.env
.env.local
.env.*.local

# runtime artifacts
.macrokit/
*.log

# Pattern-report inbox (auto-implementer input). Lives at parent dir
# ~/Documents/macrokit/.inbox/ but listed here as belt-and-suspenders in
# case a script ever writes into core/.inbox/.
.inbox/

# private leakage deny-terms (Sacred Rule #1 identifiers; never commit)
scripts/.leakage-terms.local
